/**
*   This contains the basic function that takes the path of the folder and start doing the 3 operations using external or internal functions
*   Main operations :
*   1) Reading the file for directory
*   2) if(file): loads the file to a linked list; if(Folder): than call the operator function recursively passing a new path
*    3)Encrypt/Decrypt the list
**/

